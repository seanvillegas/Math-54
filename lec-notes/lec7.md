## 2.2

Ax = b, Ax = c, Ax = d

Solve all of the above. How? 


Professor pointed out:
Any n*n matrix AI_n = I_n A = A

Where A^{-1} = A inverse
A^{-1}A = AA^{-1} = I_n
Not communitive 

Assumes:

\exists B s.t. AB=I_n. Thus B = A^{-1}

AB = I_n \right arrow B = A^{-1}

Def:
inverse only defined for square matrices because of one to one rule \land onto rule

Suppose AB = I_m. Can we say BA = I_n? No. 

Prof says that pivot in every column doesnt prove why A n*n if inverse exists AND is unique

We cannot assume either A or B is zero. Needs more proof. Examples given:

Ab = AC = I_n

AB - AC = 0 
A(B-C) = 0
A^{-1}A(B-C) = 0
B - C = 0 \rightarrow B = C


Inverse A^{-1}, invertible and nonsingular 

Page 178 Theorem 2

What more in matrix A is more telling?

ad-bc = determinant of A

when ad-bc \neq 0, ad-bc=0, matrix is not inveritable 

A is invertible iff it has the same number of n pivots -- same as row equivalent definition theorem 6? 
