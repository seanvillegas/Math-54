## Lec 1.9 & 2.0

We proved x \rightarrow Ax. Where x = R^n and Ax = R^m. I.e. linear translation

W.T.S. that for any T(x) = Ax \forall x. I.e. we they both are reciprocal (our inverses)


One-to-one restricts input and output. Saying its unique. The output comes from the input, forall. 


We say f is onto if \forall b \in B. I.e. b is not only codomain, it is range. 

E.g., it is not a good assumption to say that R^n \rightarrow R^m, where R^m is the codomain 

Standard vectors are when n = m and REF form, linearly independent, trivial. 

T(1, 0) = 1, 2, 3 T(0,1) (4, 6, 9). What is T(5, 7)
- solve with the linear formula 
- can set up matrices and show it. 
- real proof uses constants to show same output for any T(a, b)
- Every linear transformation is a matrix transformation, but its not applicable everywhere. Only to R^n \rightarrow R^m


For f: A \rightarrow B how do we tell if f is one to one:

Direct proof, assume f(a_1) = f(a_2) \implies a_1 = a_2

Counter example, \contradiction 
a_1 and a_2 are the same, then assumption is true. But if they arent, contradiction 



For onto and one-to-one, we both use the fact that they need a pivot in every column or are linearly independent, nontrivial? 

T composed of S is allowed when we have two variables of R^p \rightarrow R^n, T: R^n \rightarrow R^m. Where the codomain of S is the domain of T. Thus we can check linear transformation

We use the axiom that if its a linear transformation then its a matrix

Thus how do we shos that? 

T o S (x) = T(s(x)) = T(Bx)
= A(Bx)
= (AB)x 

(1) Note that we need to multiply the vectors A and B, and thus we need to understand how to multiply columns > 1 in vectors to prove that statement 


Just like we have the axiom that R^3 plus R^5 is not allowed. The R^n needs to match



## 2.0

Same ideas discussed in chapter 4

- Explaining manipulation of (1)
- diagonal matrix if every a_ijth is diagonal entry is 0. 
- diagonal entries are a_i_j