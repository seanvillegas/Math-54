x = x_p [when you plug 0 as the free variable] + x []_{homogeneous}


# justify this in midterm 
Let $v_1, ... , v_p$ be linearly independent vectors in $\mathbb{R}^n$. Show that $p \leq n$

Show linear independence of vectors $ \in \mathbb{R}^n 
\forall a_n$ within $A$, the condition of vectors within A all the way to $p$, require that vectors $v_p$ needs $p$ pivots


$\mathbb{R}^n \to \mathbb{R}^m \text{ is one to one implies }m \geq n$

Onto if the columns of $A$ span the codomain because $Ax = b$ has solution $\forall b$


# one true or false question multiple choice 


Linear Transformations:
For one-to-one, to prove you just check Ax = 0 has one solution

* prof called it "standard ......" 
* Also implies that A is $m \times n $

Which maps to $R^2 \to R^3$ are linear transf.

(three of some kind:)
e.g.
(x_1, x_2) \mapsto (x^2_1, x_2, x_1x_2)

Then she wrote it in an augmented martrix form, need to check

- Did some weird calculation? I think they multiplied the vectors 
[(v_1)... (vp)]

## Proving invertibility
If $A$ is upper triangular, then $A^{-1}$ is upper triangular, i.e. you can pair up $A$ $A^{-1}$ and perform operations s.t. $A$ on the left becomes the rank

## V true
Ax = 0 means infinitely many solutions

Any two numbers are linearly dependent, one vector 

Counter example:
[1 2] [1 // 1] = [3]


## Final Question

Show that if all entries of A are integers and det A = ±1 then all entries of A^{-1}

Check the adjugate A logic. Review


## Whenever you have rectangle it goes to paralleliped 
